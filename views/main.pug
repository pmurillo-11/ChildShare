extends layout_common

block content
  script(type="module" src='./javascripts/main.js')

  nav.nav-extended(style="background-color: #91C1AA;")
    .nav-wrapper(style="height: 100px;")
      img.brand-logo#Logo.right(src='images/childshare_5.png', alt='Logo', style="height: 300px;")
      ul.left.hideonmed-and-down#nav-mobile(style="height: 100%;")
        li.navTitle(style="height: 100%; margin-left: 200px;") Mission
        li.navTitle(style="height: 100%; margin-left: 200px;") About Us
        li.navTitle.active(style="height: 100%; margin-left: 200px;") Get Matched
        li.navTitle(style="height: 100%; margin-left: 200px;")
          a.valign-wrapper(href="profile" style="height: 100%; font-size: 22px;") Profile


  body
    .container
      .row.center
        h5 Schedule Child Share
      .row.center(style="margin-top: 20px")
        .col.s2
        .col.s4
          <input type="text" class="datepicker" id="datepicker">
        .col.s4
          <input type="text" class="timepicker" id="timepicker" placeholder = "select time" style="color: black;">


      .row
        #scriptList

      <header>
      <h1>Community Chat</h1>
      </header>

      <section class="chat">
        <p><strong>Pepito</strong>: It takes a community to...</p>
        <p><strong>Priscilla</strong>: raise a child！</p>
      </section>

      <form>
        <input type="text" placeholder="Say something nice" />
        <button>Send</button>
      </form>

      <!-- Include jquery, cookies, socket.io (client-side) and your own code -->
      <script src="https://unpkg.com/socket.io-client@2/dist/socket.io.slim.js"></script>
      <script src="javascript.js"></script>

  footer
    .footer-copyright(style="margin-top: 50px;")
      p.footerText 2019 ChildShare

